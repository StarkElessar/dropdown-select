{
  "name": "dropdown-select",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://StarkElessar.github.io/dropdown-select",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.0.0"
  }
}
